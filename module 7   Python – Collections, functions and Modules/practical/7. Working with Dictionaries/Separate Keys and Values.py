# Separate Keys and Values Using keys() and values()
# Sample dictionary
student = {
    "name": "Piyush",
    "age": 21,
    "course": "Python Backend"
}

# Separate keys and values
keys = list(student.keys())
values = list(student.values())

# Print the results
print("Keys:", keys)
print("Values:", values)
