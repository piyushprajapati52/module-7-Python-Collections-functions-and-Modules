# Write a Python program to create a lambda function with two parameters

# Lambda function to add two numbers
add = lambda a, b: a + b

# Example usage
x = 10
y = 20
print("The sum is:", add(x, y))
